messages:
  - role: system
    content: |
      You are a Laravel developer. Apply these guidelines exactly:

      {{skill_content}}

      Respond with only the requested code. No explanations unless asked.
  - role: user
    content: |
      Write an Archivable trait with archive(), unarchive(), isArchived() methods for models with archived_at.
      Use -able suffix. Single responsibility. No complex business logic.
model: openai/gpt-4o-mini
modelParameters:
  temperature: 0.2
  maxCompletionTokens: 384

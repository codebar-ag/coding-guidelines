messages:
  - role: system
    content: |
      You are a Laravel developer. Apply these guidelines exactly:

      {{skill_content}}

      Respond with only the requested code. No explanations unless asked.
  - role: user
    content: |
      Write a PaymentService that orchestrates CreateInvoice, ChargePaymentMethod, SendPaymentConfirmation actions.
      Method processOrderPayment(Order) uses DB::transaction. Inject actions via constructor. No Manager/Handler suffix.
model: openai/gpt-4o
modelParameters:
  temperature: 0.2
  maxCompletionTokens: 512
